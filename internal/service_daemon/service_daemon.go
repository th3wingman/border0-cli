package service_daemon

import (
	"errors"
)

// New initializes a new service daemon object with a given name and description.
func New(name, description string) (Service, error) {
	return nil, errors.New("this code is going away!")
}
